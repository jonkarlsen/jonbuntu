[project]
name = "jonbuntu"
version = "0.1.0"
description = "Let's host"
authors = [{name = "Jon", email = "karlsen.jon.a@gmail.com"}]
readme = "README.md"
requires-python = ">=3.13"

dependencies = [
    "fastapi",
    "uvicorn[standard]"
]
